[build]
builder = "DOCKERFILE"
dockerfilePath = "api/Dockerfile"
watchPatterns = ["api/src/**", "api/migrations/**", "api/Cargo.toml", "api/Cargo.lock", "api/Dockerfile"]

[deploy]
restartPolicyType = "ON_FAILURE"
